# ───── 공통 ─────
node_modules/
.pnpm-store/          # pnpm 캐시
.env*                 # .env, .env.local, …
dist/
build/
coverage/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
.DS_Store             # macOS
Thumbs.db             # Windows
.idea/                # JetBrains IDE
.vscode/              # VS Code 워크스페이스 설정
*.swp                 # Vim/Neovim 스왑 파일
*.swo
*.orig                # 충돌 해결 잔여물

# ───── 프런트엔드(client) ─────
client/.cache/        # Vite 캐시
client/.vite/

# ───── 백엔드(server) ─────
server/logs/
server/tmp/

# ───── 테스트 & 빌드 툴 ─────
coverage/
.jest/
.nyc_output/
playwright-report/
cypress/videos/
cypress/screenshots/

# ───── Docker / CI 산출물 ─────
docker-compose.override.yml
*.local.yml
*.tgz
*.tar

# ───── OS 별 추가 ─────
# Linux
*~                    # 백업 파일
# macOS
.AppleDouble
.LSOverride
